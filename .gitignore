# OS
.DS_Store

# BMad per-machine personal settings (not shared across team)
_bmad/config.user.toml
_bmad/config.user.yaml

# bmad-loop runtime state only; policy.toml + hook script ARE tracked
.bmad-loop/runs/
.bmad-loop/cache/

# bmad skill render scratch (intermediate workflow renderings, regenerable)
_bmad/render/

# Dependencies
node_modules/

# Build output
dist/
build/
.next/
out/
.turbo/
*.tsbuildinfo

# Prisma generated client (regenerated by `db:generate`; not committed)
packages/core/generated/

# Env (only .env.example is tracked)
.env
.env.local
.env.*.local

# Test artifacts
coverage/
playwright-report/
test-results/
.playwright/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/settings.json

# Claude Code scheduled-task runtime state (cron; changes on every fire)
.claude/scheduled_tasks.json
.claude/scheduled_tasks.lock
