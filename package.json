{
  "name": "aguhot",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.2.0",
  "engines": {
    "node": "24.18.0",
    "pnpm": ">=10.2.0"
  },
  "scripts": {
    "dev": "pnpm dev:deps || exit $?; test -f .env || { echo 'Missing .env; copy .env.example first.' >&2; exit 1; }; set -a; . ./.env; set +a; pnpm --stream --parallel --filter @aguhot/web --filter @aguhot/worker run dev",
    "dev:deps": "curl --noproxy '*' -fsS http://127.0.0.1:1200/ >/dev/null 2>&1 || docker compose up -d --wait --wait-timeout 60 rsshub",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "lint:root": "eslint .",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,css}\" --ignore-path .gitignore",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md,css}\" --ignore-path .gitignore"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^10.0.0",
    "prettier": "^3.9.4",
    "typescript-eslint": "^8.63.0"
  }
}
