/**
 * @aguhot/worker — ingest / normalize / cluster / explain / publish runtime.
 *
 * Per Story 1.1, this is only a placeholder entry. The BullMQ queues and
 * jobs (ingest / cluster / explain / publish / digest) are delivered in
 * Stories 1.4–1.10, per ARCHITECTURE-SPINE.md Structural Seed.
 *
 * Kept import-free so the skeleton typechecks without DB/Redis wiring.
 */
export {};
